services:
  lvnotes:
    build: .
    container_name: lvnotes
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - lvnotes_data:/app/data
    restart: unless-stopped

volumes:
  lvnotes_data:
